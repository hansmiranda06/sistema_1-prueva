<?php require_once '../../config/conexion.php';$fecha=$_POST['fecha']??date('Y-m-d');$producto=$_POST['producto']??'';$cantidad=(float)$_POST['cantidad'];$obs=trim($_POST['observaciones']??'');$s=$conn->prepare('INSERT INTO produccion (fecha,producto,cantidad_m3,observaciones) VALUES (?,?,?,?)');$s->bind_param('ssds',$fecha,$producto,$cantidad,$obs);$s->execute();header('Location: index.php');exit;
